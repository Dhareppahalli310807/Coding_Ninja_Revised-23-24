function lowerCaseString(firstName,lastName){
    let answer;
// You have been provided with two strings
    let lowercaseFirstName = firstName.toLowerCase();
    let lowercaseLastName = lastName.toLowerCase();

    // Combine the lowercase first name and last name
    let username = lowercaseFirstName + lowercaseLastName;

    return username;
    

    return answer;
}