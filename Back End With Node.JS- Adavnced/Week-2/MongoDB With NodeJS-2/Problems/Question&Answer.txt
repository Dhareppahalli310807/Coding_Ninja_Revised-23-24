1)What is the default indexing behavior for MongoDB when creating an index?
--> Ascending indexing

2)How would you create a compound index in MongoDB using the createIndex() method?
--> createIndex({field1:1, field2:1})

3)What is the primary purpose of using transactions in a database?
--> insuring data consistency and integrety

4)Which of the following scenarios is best suited for using transactions?
--> processing finacial transactions between bank account

5)In MongoDB transactions, what is the primary role of the session object?
--> to represents the logical transactions context

6)